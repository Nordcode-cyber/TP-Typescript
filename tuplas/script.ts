(()  => {
    const lenguajes:[string,number,string, boolean] = ['Kotlin',120,'Android', true]; 
    console.log(lenguajes);
    console.table(lenguajes);
    const naves:[string, number,string, number] = ['Navio',11,'la mara',25];
    console.log(naves);
    console.table(naves);

})()